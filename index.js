import { run } from '@erutidians/auto-dop';

// change this.
const user = {
  name: 'Adi Prastiyo',      // change this to any name that you're comfortable with.
  github: 'adiprass28',      // your github username.
  start: '11/19/2022',    // the date that you will start this challenge (e.g. m/d/yyyy)
  round: 1,             // the round number you currently are in. (if omitted, defaults to 1)
};

run(user);
